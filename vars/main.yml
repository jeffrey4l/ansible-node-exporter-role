---
go_arch_map:
  i386: '386'
  x86_64: 'amd64'
  aarch64: 'arm64'
  armv7l: 'armv7'
  armv6l: 'armv6'

node_exporter_checksums:
  0.18.1: "{{ lookup('file', role_path ~ '/vars/versions/v0.18.1.sha256sums.yml')|from_yaml }}"
  0.18.0: "{{ lookup('file', role_path ~ '/vars/versions/v0.18.0.sha256sums.yml')|from_yaml }}"
  0.17.0: "{{ lookup('file', role_path ~ '/vars/versions/v0.17.0.sha256sums.yml')|from_yaml }}"
  0.16.0: "{{ lookup('file', role_path ~ '/vars/versions/v0.16.0.sha256sums.yml')|from_yaml }}"
  0.15.2: "{{ lookup('file', role_path ~ '/vars/versions/v0.15.2.sha256sums.yml')|from_yaml }}"
  0.15.1: "{{ lookup('file', role_path ~ '/vars/versions/v0.15.1.sha256sums.yml')|from_yaml }}"
  0.15.0: "{{ lookup('file', role_path ~ '/vars/versions/v0.15.0.sha256sums.yml')|from_yaml }}"
